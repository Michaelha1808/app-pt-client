[www]
user = www-data
group = www-data
listen = 9000

; Dynamic: spawn worker khi cần, kill khi rảnh
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
; Restart worker sau 500 request để tránh memory leak
pm.max_requests = 500
