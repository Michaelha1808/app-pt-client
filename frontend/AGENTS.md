# Frontend agent rules

This frontend uses Nuxt 3 with Vue single-file components.

- Put application UI under `src/app.vue`, `src/components`, `src/composables`, and `src/utils`.
- Use `NUXT_PUBLIC_API_URL` for the public API base URL.
- Keep styling compatible with Tailwind CSS 4 via `src/assets/css/main.css`.
